

import 'package:flutter/material.dart';

class KDialog extends StatelessWidget {
  const KDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(),
    body: Container()
    );
  }
}
